{
  "name": "lambda-get-common-name",
  "version": "1.0.0",
  "description": "AWS Lambda that extracts the Common Name (CN) from a PEM certificate",
  "type": "module",
  "main": "index.mjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "pem",
    "certificate",
    "x509"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pem": "^1.14.8"
  }
}
